    type Query {
        userById(id: String): User
    }

    input UserInfo {
        email: String!
        password: String!
    }

    type Mutation {
        register(info: UserInfo): User!
    }

    type User {
        id: ID!
        email: String!
        password: String!
    }